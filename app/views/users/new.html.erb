<img src="/gfx/frog.jpg">

<div class="panel right">
  <h1>New to GameTime Banter?</h1>
  </break>
  <h4>Sign up! You get to see fan's in-game reactions. Sweating those clutch free-throws? Share in the collective anxiety. Can't see the penalty kick? Share the pain. GameTime Banter is here to bring you closer to the pulse of the fan.</h4>
  </break>
  <%= form_for @user do |f| %>
    <% if @user.errors.any? %>
    <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
    <% end %>

    <%= f.text_field :email, placeholder: "email" %>
    <%= f.text_field :username, placeholder: "username" %>
    <%= f.text_field :name, placeholder: "name" %>
    <%= f.text_field :password, placeholder: "password" %>
    <%= f.text_field :password_confirmation, placeholder: "password_confirmation" %>
    <%= f.submit "Create Account" %>
    <% end %>
  </div>

